openapi: 3.0.3
info:
  title: YML Test API
  description: Test API specification in YML format
  version: 1.0.0
servers:
  - url: https://api.example.com/v1
paths:
  /status:
    get:
      summary: Status check
      operationId: statusCheck
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
